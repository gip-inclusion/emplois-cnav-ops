apiVersion: v1
kind: Service
metadata:
  name: {{ include "jetonv3.fullname" . }}
  labels:
    {{- include "jetonv3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jetonv3.selectorLabels" . | nindent 4 }}
